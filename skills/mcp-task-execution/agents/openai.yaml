interface:
  display_name: "MCP Task Execution"
  short_description: "Execute MCP tasks and publish honest reports"
  default_prompt: "Use $mcp-task-execution for the MCP task in this request."
policy:
  allow_implicit_invocation: true
