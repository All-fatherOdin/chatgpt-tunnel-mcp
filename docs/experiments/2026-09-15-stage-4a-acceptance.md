# Приёмка этапа 4A — 2026-09-15

Среда: Windows, Node.js, codex-cli 0.146.0, локальный Codex App Server по stdio. Исполнитель: gpt-5.6-sol, reasoningEffort=medium, workspace-write, approvalPolicy=never.

## Реальный модельный запуск

Временный проект, отдельные SQLite-базы exchange и dispatcher. Planner-сервис создал Task с autoStart=true и разрешённым scope docs. Диспетчер выполнил claim, создал thread, запустил модель и доставил валидный итоговый Report. Planner-сервис прочитал отчёт. Тест отдельно прочитал созданный docs/acceptance.txt и сверил содержимое stage-4a-ok с завершающим переводом строки.

- taskId: `8459c151-5651-412f-898f-a23588a5da8d`
- reportId: `93705246-0468-4214-83a1-6dd579347e3d`
- threadId: `01a0a3df-7534-7d11-9930-49465a4726f2`
- Фактически возвращённая модель: `gpt-5.6-sol`.
- Итог: passed; 72,6 секунды.

Это реальный ход модели и реальные изменения файлов во временном проекте, не mock. Временный проект и его тестовые базы удалены тестовым harness после проверки; приведённые ID фиксируют исторический прогон и не относятся к действующей exchange-базе. История Codex не удалялась.

Два предшествующих пробных запуска завершились ошибкой Structured Outputs: регулярное выражение path использовало неподдерживаемый lookaround. В генерационной схеме оно заменено описанием и обычной строкой; локальная строгая проверка safe-relative-path сохранена. Успешный прогон проведён после исправления.

## Что проверяется отдельно

Итоговый автоматический прогон: build/typecheck прошли; 28 тестов MCP/dispatcher и 6 workflow-тестов прошли. Live-тест пропускается в обычном npm test и запускался отдельно, как описано выше.

Автотесты с имитацией исполнителя проверяют потерю ответа после claim/submit, повтор точного запроса, recovery прежнего хода, смену сессии и отказ от повторного выполнения при неоднозначности. Протокольный stdio fixture проверяет ситуацию, когда события завершения приходят раньше ответа turn/start. Эти проверки не выдаются за реальные аварии модельного процесса.

Ручной UI-цикл ChatGPT → диспетчер → ChatGPT в этом прогоне не выполнялся. Автоматическое пробуждение ChatGPT, ревью/принятие результата, Windows-служба и включение диспетчера для действующих пользовательских проектов не входят в эту приёмку. Локальные planner/worker-конфиги и существующий туннель автоматически не переключались.
